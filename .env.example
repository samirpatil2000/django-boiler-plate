# Django settings
DEBUG=True
SECRET_KEY=your_secret_key_here
ALLOWED_HOSTS=localhost,127.0.0.1
DISABLE_AUTH=False

# Database settings
# Choose either 'postgresql' or 'sqlite3'
DB_ENGINE=sqlite3

# PostgreSQL settings (only needed if DB_ENGINE=postgresql)
DB_NAME=django_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# SQLite3 settings (default - no additional config needed)
# When DB_ENGINE=sqlite3, database file will be created as db.sqlite3
